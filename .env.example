# =============================================================================
# SOLOBOOKS FRONTEND ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env.local for local development
# For production, set these variables in your hosting platform (Vercel, AWS, etc.)
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API base URL
# Development: http://localhost:3000
# Production: Your production API URL
NEXT_PUBLIC_API_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Authentication & OAuth
# -----------------------------------------------------------------------------
# Google OAuth Client ID for Google Sign-In
# Get from: https://console.cloud.google.com/apis/credentials
# Required for: Google authentication on login/register pages
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

# -----------------------------------------------------------------------------
# Payment Processing
# -----------------------------------------------------------------------------
# Stripe Publishable Key for payment method setup
# Get from: https://dashboard.stripe.com/apikeys
# Required for: Subscription payments and payment method management
# Use test key (pk_test_...) for development, live key (pk_live_...) for production
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-stripe-publishable-key-here

# -----------------------------------------------------------------------------
# Error Tracking & Monitoring
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking and monitoring
# Get from: https://sentry.io/settings/YOUR_ORG/projects/YOUR_PROJECT/keys/
# Optional but highly recommended for production
# Leave empty to disable Sentry
NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn@sentry.io/your-project-id

# -----------------------------------------------------------------------------
# Build Configuration (Set by hosting platform - do not add to .env.local)
# -----------------------------------------------------------------------------
# NODE_ENV=production                    # Set by Next.js automatically
# NEXT_RUNTIME=nodejs                    # Set by Next.js automatically
# SENTRY_AUTH_TOKEN=your-sentry-token    # Only needed for Sentry source map uploads (CI/CD)

# -----------------------------------------------------------------------------
# Optional: Sentry Build Plugin (CI/CD only)
# -----------------------------------------------------------------------------
# These are only needed when building with Sentry source map uploads
# Usually set in CI/CD environment, not in .env.local
# SENTRY_ORG=solobooks-i4
# SENTRY_PROJECT=soloboks-frontend
# SENTRY_AUTH_TOKEN=your-sentry-auth-token
